
/**
 * This bit grabs any div with the class 'transparent'
 * and brings in each child element one after the other
 * with a 'delay' millisecond delay per element making the
 * animations consecutive
 */
(function($) {

 
})(jQuery);

// Common intro
$(function() {

	// Go ahead and fade in anything that has the class
	


	/*$('.content').masonry({
                       itemSelector : '.item',
                       isAnimated: true,
                       isFitWidth: true
                       });
*/
 //$('#content').fadeIn();
//$('.content').masonry('reload');
});

